apiVersion: v1
kind: Service
metadata:
  name: python-app-service
  namespace: github-copilot-ns
  labels:
    app: python-app
    environment: production
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: python-app
