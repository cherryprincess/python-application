apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: github-copilot-ns
  labels:
    app: python-flask-app
    environment: production
data:
  DEBUG: "false"
  HOST: "0.0.0.0"
  PORT: "8080"
  ENVIRONMENT: "production"
  LOG_LEVEL: "INFO"
